# Abhängigkeiten
/node_modules

# Next.js
/.next
/out
next-env.d.ts

# Zugangsdaten — NIEMALS committen
.env
.env.local
.env.*.local

# Broker Control Plane (Task 08): verschluesselte Credential-Envelopes
# (Datei-Fallback des Secret-Stores; niemals Klartext, niemals committen)
/data/secrets

# Betrieb
*.log
/backups
/models
*.gguf

# System
.DS_Store

# Build-Kompilat
*.tsbuildinfo

# Universe-Registry: Instrumentendatei ist versioniert, das Audit-Log nicht
/data/universe/audit-log.ndjson
/data/universe/*.tmp

# Historical Store (Task 03): append-only OHLCV, läuft lokal, nicht versioniert
/data/history
/data/history/*.tmp

# Marktdaten-Fehler-Manifest (MDERR-006): Laufzeit-Artefakt des Sync-Jobs,
# vom Scanner/Operations Center gelesen — nicht versioniert
/data/market-data-errors.json
/data/market-data-errors.json.tmp

# Model Router (Task 09): Routing-Audit und Modi-Persistenz (Laufzeitdaten)
/data/routing

# Live-Gate (Task 11): State-Files, Audit-Hash-Kette, Kill-Failsafe-Datei
# und Security-Suite-Stamp sind Laufzeit-/Deployment-Artefakte (aus CI),
# niemals versioniert.
/data/live-gate
/data/live-gate-test

# Scanner-Artefakte (Task 04): taegliche Universe-Snapshots werden lokal
# erzeugt und sind aus Registry + Historie reproduzierbar — nicht versioniert.
/artifacts
