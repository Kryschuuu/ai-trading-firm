# Remediation-Tracking — Feature-Gap-Analyse (2026-09-18)

Diese Datei ist die **einzige Wahrheit** für den Status aller Findings
(GAP-01…GAP-10) dieses Audits. Sie wird von der Arena-Session aktualisiert,
die den jeweiligen Prompt ([`../prompts/README.md`](../prompts/README.md))
abgearbeitet hat — im selben PR wie die Implementation.

## Status-Tabelle

| ID | Titel | Nutzen | Status | Fix-Version | PR / Branch | Notizen | Prompt |
|----|-------|--------|--------|-------------|-------------|---------|--------|
| GAP-01 | Backtesting-Engine mit Walk-Forward-Validierung (ohne Lookahead) | ★★★★★ | OPEN | — | — | synthetischer Fallback in `backtestStep.ts` mitentfernen (fail-closed) | [PROMPT-01](../prompts/PROMPT-01-backtesting-walk-forward.md) |
| GAP-02 | Realistische Execution-Simulation (Slippage/Fees/**Funding**) | ★★★★★ | FIXED | 1.42.0 | PR [#136](https://github.com/Kryschuuu/ai-trading-firm/pull/136) · `arena/01a0b48a-ai-trading-firm` | D1–D4 umgesetzt (Funding-Accrual im Monitor-Tick, `positions.funding_paid`, Kalibrierungs-Flags, Determinismus-Tests); Rate-Default 0 = neutral. Offen: echte Funding-Raten-Anbindung (Provider-Interface vorhanden) | [PROMPT-02](../prompts/PROMPT-02-execution-simulation.md) |
| GAP-03 | Trade-Journal mit Agenten-Attribution & Feedback-Loop | ★★★★★ | IN_PROGRESS | 1.43.0 | PR [#137](https://github.com/Kryschuuu/ai-trading-firm/pull/137) · `arena/01a0b4c1-ai-trading-firm` | D1–D4 in Umsetzung: append-only `trade_journal` + `journal_agent_weights`, MAE/MFE (CANDLE_GAP/NO_DATA), Bayes-Glättung α=β=2 + minSample, `JOURNAL_FEEDBACK_MODE` off (Default)/monitor/enforce, `GET /api/firm/journal`. FIXED nach Merge | [PROMPT-03](../prompts/PROMPT-03-trade-journal-attribution.md) |
| GAP-04 | Vol-Sizing + Korrelations-Exposure-Limits | ★★★★★ | IN_PROGRESS | 1.48.0 | `arena/01a0b953-ai-trading-firm` | D1–D3 umgesetzt: `src/lib/positionSizing.ts` (reine `computePositionSize()`: qty = Risikobudget/Stop-Abstand, Stop-Auflösung EXPLICIT → ATR-Fallback `k·ATR` (`RISK_ATR_STOP_MULT` 2, Bounds [0.5,6]) → UNKNOWN-Basis-Größe + Kennzeichnung + Audit (Muster adaptiveRisk v1.36.21, kein Block); Fractional-Kelly-Deckel `RISK_KELLY_FRACTION` (Default 0 = aus, Bounds [0,1]) wirkt nur mit Journal-Stats (GAP-03, `JOURNAL_MIN_TRADES`-Gate, Payoff>0), sonst dokumentiert wirkungslos; Ergebnis immer an LIMIT_CEILINGS/maxPositionPct geklemmt — Sizing verschärft, lockert nie; `atr()` (absolute) neu in `indicators.ts`; verdrahtet in Engine + Mikro-Executor (byte-identisch mit Defaults, nur 0-Stop-Regeln profitieren: ATR-Fallback statt Stop am Entry)). D2 `src/lib/clusterExposure.ts` (Guardrail-Schicht 3): `correlationMatrix`/`correlationClusters` **aus `src/portfolio` importiert** (keine Duplikation), log-Renditen über gemeinsame Zeitstempel aus dem HistoricalStore (1h, Fenster `RISK_CORR_WINDOW_CANDLES` 90, Bounds [30,365]), Single-Linkage `|ρ| ≥ RISK_CORR_THRESHOLD` (0.7, Bounds [0.3,0.99]), `RISK_MAX_PER_CLUSTER` (3, Bounds [1,10]) → `cluster-exposure:max-per-cluster:N`; TTL-Cache `RISK_CORR_CACHE_TTL_MS` (900000, Bounds [60000,3600000]), Berechnung nur je Order-Prüfung, kein Hintergrund-Job; Stale-Policy fail-closed (keine/zu alte Kerzen, <20 gemeinsame Renditen, unauflösbares Symbol → enforce: `cluster-exposure:correlation-stale`, statt raten). D3: `RISK_CLUSTER_LIMITS_MODE` monitor (**Default**: nur Audit-Notiz `CLUSTER_EXPOSURE_MONITOR` + Log mit Würde-Prüfung `wouldBlock: true`) \| enforce (echte Ablehnung `CLUSTER_EXPOSURE_BLOCKED`), unbekannt → monitor + Warnung; Audit je Guardrail-Entscheidung (security-Klasse, at-least-once); `GET /api/firm/risk` zeigt effektive Sizing-/Cluster-Parameter + Kelly-Edge-Status + UNKNOWN-Zustände. Tests: `tests/positionSizing.test.ts` (14), `tests/riskGuard.cluster.test.ts` (14, inkl. Cache-TTL Fake-Clock, 0.699-vs-0.7-Grenzfälle); bestehende riskGuard-/portfolio-/micro-Tests unverändert grün (Defaults). Abweichung dokumentiert: Kelly-Edge-Quelle = Trade-Journal (GAP-03, vorhanden); `ruleExecutions` trägt kein P&L und kann keinen Payoff liefern — bleibt als dokumentierte Alternative ohne Wirkung. FIXED nach Merge | [PROMPT-04](../prompts/PROMPT-04-vol-sizing-correlation-limits.md) |
| GAP-05 | Server-seitiges Exit-Management (Trailing/Time-Stop/OCO) | ★★★★★ | IN_PROGRESS | 1.44.0 | PR [#138](https://github.com/Kryschuuu/ai-trading-firm/pull/138) · `arena/01a0b5f8-ai-trading-firm` | D1–D4 umgesetzt: `src/lib/exits.ts` (reine `decideExit`-Logik, Bounds-Clamp, Defaults aus = identisches Verhalten), Trailing-Zustand crash-safe in `positions.trailing_armed`/`trailing_stop` (`drizzle/2026-09-18_exit_management.sql`), Time-Stop `RISK_TIME_STOP_HOURS` (0 = aus), OCO via atomarem DB-Claim (`UPDATE … WHERE status='OPEN'` RETURNING, `applyExit()`) — genau ein Exit auch prozessübergreifend, Audit je Exit (`exit:SYMBOL:grund`), `tests/monitor.exits.test.ts` (17 Tests inkl. Race + Restart-Persistenz). Abweichung dokumentiert: OCO war nicht nur „unbelegt“, sondern fehlte prozessübergreifend; Config im `loadFundingConfig`-Muster (Env) statt Dashboard-Namensraum (Begründung im Finding). FIXED nach Merge | [PROMPT-05](../prompts/PROMPT-05-server-side-exit-management.md) |
| GAP-06 | Regime-Detection als Gate für Agenten-Gewichtung | ★★★★ | IN_PROGRESS | 1.46.0 | `arena/01a0b708-ai-trading-firm` | D1–D3 umgesetzt: `src/lib/marketRegime.ts` (deterministischer Klassifikator TREND_UP/TREND_DOWN/RANGE/HIGH_VOL/CRASH aus ADX+Slope+Vol-Perzentil+Drawdown, Priorität CRASH > HIGH_VOL > TREND_* > RANGE, Hysterese `MarketRegimeStateMachine` nach `adaptiveRisk`-Muster), ADX (Wilder) neu in `src/lib/indicators.ts`; Gate als Datenkontext (kein Veto): `REGIME_GATE_MODE` off/monitor (**Default**)/enforce, enforce dämpft Engine-Risikobudget-Kontext + Mikro-Executor-Sizing (stets gegen Code-Ceilings); UNKNOWN → Faktor 1 + Kennzeichnung; Ops-Center-Risk-Sektion je Instrument, `regime-history.json` in Cycle-Artefakten, Audit `regime:SYMBOL:VON→NACH` + `REGIME_GATE_APPLIED`. Strategieklasse deterministisch aus Mission-Template (Regeln ohne Mission → Faktor 1; Zyklus-Step-Anreicherung bewusst offen, s. Finding). FIXED nach Merge | [PROMPT-06](../prompts/PROMPT-06-regime-gate.md) |
| GAP-07 | Datenqualitäts-Layer & Multi-Timeframe-Konsistenz | ★★★★ | IN_PROGRESS | 1.47.0 | `arena/01a0b751-ai-trading-firm` | D1–D4 umgesetzt: `src/marketdata/quality.ts` (GAP/OUTLIER/INVALID/DUPLICATE + neue MDERR-IDs `QUALITY_*` in `marketDataErrors.ts`; Report `data/marketdata/quality-report.json` via `resolveRuntimePath`, Metrik `market_data_quality_findings_total`, Log-Zeile; Modi `MARKETDATA_QUALITY_MODE` log (Default, nur sichtbar)/strict (fail-closed: INVALID ⇒ `DATA_UNAVAILABLE` über die bestehende Stale-Fallback-Kette, verdrahtet in `run-scan.ts` + `ScannerService.refresh`); Outlier-Schwelle `MARKETDATA_OUTLIER_ATR_MULT` 25×leave-one-out-TR-Baseline, Bounds [5,200], exakter Grenzwert = kein Befund, Flash-Move-Schutz getestet), D2 Stale-Guard (`MARKETDATA_STALE_{1H,4H,1D}_HOURS` 26/104/624 mit Bounds, Ausweis als Zähler im Sync-Status, keine Symbole), D3 `src/marketdata/aggregate.ts` (1h→4h/1d deterministisch, UTC-Anker 00/04/…/00:00, unvollständige Bucket NIE aggregiert, Zeitmaske, Konsistenz-Check; `MARKET_SYNC_AGGREGATE`/`--aggregate` Default off, Aggregat als neue Reihe `feed: agg:1h`, 1h-Quelle unangetastet), D4 Cross-Check nur Interface + Vertrag (optionales `getCrosscheckCandles()` am `MarketDataAdapter`, `MARKETDATA_CROSSCHECK` off, Toleranz 1 %, Bounds [0.1,10]; keine neue Venue-Anbindung — no-op ohne Implementierung). Historie-Immutabilität + Freeze-Tests; `QUALITY_*` fließt nie ins Fetch-Manifest (log-Semantik). Tests: `test/marketdata/quality.test.ts` (35), `test/marketdata/aggregate.test.ts` (14), CLI `--aggregate`. FIXED nach Merge | [PROMPT-07](../prompts/PROMPT-07-data-quality-multi-timeframe.md) |
| GAP-08 | LLM-Output-Validierung & Prompt-Eval-Harness | ★★★★ | IN_PROGRESS | 1.49.0 | `arena/01a0b9e6-ai-trading-firm` | D1–D3 umgesetzt: `src/cycle/plausibility.ts` (Plausibilitäts-Schicht nach Schema-Validierung: MONOTONICITY je Richtung, PRICE_RANGE ±15 % [1, 90] um Known-Good-Kurs, RATIONALE_MISSING bei Confidence ≥ 0.9 (40 Zeichen [0, 1000], 0 = aus), HALLUCINATED_PRICE regex-Zahlenbezug; strukturierte `{code, field, detail}`; genau EIN Retry mit Fehlermeldungs-Kontext, danach deterministischer Skip mit `CYCLE_STEP_SKIPPED` `plausibility:CODE` + sichtbarem `plausibility`-Block in `07-research.json`/`02-macro-analyst.json`; auch eskalierte Antworten geprüft; `referenceMissing` ohne Kerzen), verdrahtet in Research- + Makro-Step via `spec.plausibility`; D2 Eval-Harness (`npm run eval:prompts`, 12 Golden-Fixtures, Offline-Default deterministisch byte-identisch, Reports `data/eval/`, Exit 0/1/2, optionaler `--provider`-Rauchtest); D3 `src/routing/turnBudget.ts` (`TurnBudget` je Turn: `LLM_MAX_TOKENS_PER_TURN` 20000 [1000, 200000] + `LLM_MAX_TURN_MS` 120000 [10000, 900000], Aufrufgrenzen-Prüfung; Bruch → `TurnBudgetExceededError` + Audit `llm-budget:tokens`/`llm-budget:time` (`budget_blocked`), niemals Fallback-Umwandlung). Laut klemmende Flags (`envNumber`), keine neue Runtime-Dep. Tests: `tests/plausibility.test.ts` (25), `tests/turnBudget.test.ts` (9), `tests/evalPrompts.test.ts` (11); Cycle-/Routing-Bestand grün. Docs: `docs/LLM_ROUTING.md` §17, `CONFIGURATION.md`, `.env.example`, CHANGELOG 1.49.0. FIXED nach Merge | [PROMPT-08](../prompts/PROMPT-08-llm-validation-eval-harness.md) |
| GAP-09 | Reconciliation Broker ↔ DB + idempotente Order-IDs | ★★★★ | OPEN | — | — | `orderIntents` + Bitunix-Idempotenz teilweise vorhanden; Delta = generischer Job + Pause + Paper-Invarianzen | [PROMPT-09](../prompts/PROMPT-09-reconciliation-idempotency.md) |
| GAP-10 | Observability & automatische Circuit-Breaker | ★★★★ | IN_PROGRESS | 1.45.0 | PR [#139](https://github.com/Kryschuuu/ai-trading-firm/pull/139) · `arena/01a0b6a7-ai-trading-firm` | D1–D4 umgesetzt: `prometheusMetrics()` (async) mit Firmen-Metriken (Equity/Drawdown/Positionen/PnL, Fills-/Rejects-/LLM-Counter; degraded = Metrik weglassen + `# HELP … degraded`, nie Throw/Hang; Labels nur klassifizierte Codes), `src/lib/circuitBreaker.ts` (Drawdown/Tagesverlust/Verlustserie → bestehender Kill-Switch-Pfad, Grund `auto-circuit-breaker:<metrik>:<wert>`, Audit mit fixiertem Auslösewert, Alert, **Latching**, kein Auto-Re-Arm), `src/lib/alerts.ts` (Log-/File-Sink, optionaler Webhook nur aus dem Secret-Store, Debounce je Code), Heartbeat (`HEALTH_STALE_AFTER_MS`, `/api/health`) + `npm run watchdog` (alarm-first, kein Restart). Tests: `tests/telemetry.firm.test.ts` (7), `tests/circuitBreaker.test.ts` (12), `tests/alertSink.test.ts` (8), `tests/health.heartbeat.test.ts` (5). Firmenzustand serverseitig in `src/lib/firmState.ts` (telemetry.ts bleibt DB-frei wegen Client-Import-Graph). Rest-Delta: kein `/api/metrics`-Scrape-Endpoint (Exposition nur über `prometheusMetrics()`); der bestehende Drawdown-Auto-Pull im Engine-Zyklus (l.516) bleibt unverändert → beide Pfade können denselben Kill-Switch ziehen (idempotent, aber zwei Gründe im Audit). FIXED nach Merge | [PROMPT-10](../prompts/PROMPT-10-observability-circuit-breaker.md) |

Zusätzlich aus der Verifikation hervorgegangen (nicht Teil des Co-Audits):

| ID | Titel | Severity | Status | Fix-Version | PR / Branch | Notizen |
|----|-------|----------|--------|-------------|-------------|---------|
| ENV-01 | `tests/secretStore.test.ts` — Test-Isolation: „db→file-Fallback ohne DATABASE_URL“ erhält `db`, wenn eine DB erreichbar ist | LOW | OPEN | — | — | npm-Script exportiert `DATABASE_URL` prozessweit; der Fallback-Test deutet das als „DB verfügbar“. Standalone grün. Fix-Umfang: env-Inject instead of process-env-Fallback im getesteten Pfad, oder Test-Expectation an effektive URL koppeln |

## Legende

- **Status:**
  - `OPEN` — Verifiziert, noch nicht bearbeitet
  - `IN_PROGRESS` — Arena-Session läuft / PR offen (Branch angeben)
  - `PARTIAL` — Teile umgesetzt, Rest mit Begründung offen
  - `FIXED` — Umgesetzt, mit Version und PR belegt
  - `WONTFIX` — Bewusst nicht umgesetzt, mit Begründung

## Workflow je Prompt-Durchlauf

1. Arena-Session mit dem jeweiligen `PROMPT-XX`-Block starten
   (Reihenfolge siehe [`../prompts/README.md`](../prompts/README.md)).
2. Die Session implementiert das Delta, fügt Tests + Docs hinzu und liefert
   Changelog-Eintrag + Versions-Bump im selben PR.
3. Im selben PR: Zeile dieser Tabelle auf `IN_PROGRESS` → nach Merge `FIXED`
   (Version + PR-Link), Fundings-Datei
   `../findings/GAP-XX-*.md` um einen „Umsetzung“-Abschnitt ergänzen.
4. Abweichungen vom Prompt-Umfang (Scope-Kürzungen, andere Architektur) werden
   in der Notiz-Spalte begründet — kein stilles „erledigt“.
